## About This Project

This project is a from-scratch implementation of an HTTP/1.1 server and request parser in Go. It operates directly on raw TCP streams, completely avoiding the standard `net/http` library's abstractions to better understand the inner workings of web servers.
